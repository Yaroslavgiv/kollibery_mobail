# Анализ связи заказов продавца и покупателя

## Проблема
Заказ, созданный покупателем, не появляется у продавца.

## Текущая реализация

### 1. Создание заказа (`/order/placeorder`)
**Эндпоинт:** `POST http://81.3.182.146/order/placeorder`

**Отправляемые данные:**
```json
{
  "userId": "ID покупателя",
  "productId": 123,
  "quantity": 1,
  "deliveryLatitude": 59.9343,
  "deliveryLongitude": 30.3351
}
```

**⚠️ Проблема:** `sellerId` не передается в запросе. Бэкенд должен сам определить продавца по `productId`.

### 2. Получение заказов продавца
**Эндпоинт:** `GET http://81.3.182.146/order/getorders?role=seller`

**Ожидаемое поведение:**
- Бэкенд должен возвращать заказы, где товар (`productId`) принадлежит текущему продавцу
- Связь определяется через таблицу продуктов: `product.userId = seller.id`

### 3. Модель данных

**OrderModel:**
- `userId` - ID покупателя
- `productId` - ID товара
- `sellerName` - имя продавца (заполняется на клиенте)
- `sellerId` - ID продавца (добавлен в enrichedOrder из продукта)

**ProductModel:**
- `userId` - ID продавца (владельца товара)
- `id` - ID товара

## Добавленное логирование

### При создании заказа:
```
⚠️ ВНИМАНИЕ: sellerId не передается! Бэкенд должен определить продавца по productId
```

### При получении заказов:
```
📋 Обработка заказа ID: X, productId: Y
✅ Товар найден. sellerId (из продукта): Z
```

## Рекомендации для проверки бэкенда

1. **Проверить эндпоинт `/order/placeorder`:**
   - Должен автоматически определять `sellerId` по `productId` из таблицы продуктов
   - Должен сохранять связь заказа с продавцом в БД

2. **Проверить эндпоинт `/order/getorders?role=seller`:**
   - Должен фильтровать заказы по текущему пользователю (из токена)
   - Должен возвращать только заказы, где товар принадлежит текущему продавцу
   - Фильтрация: `orders.productId IN (SELECT id FROM products WHERE userId = current_user_id)`

3. **Проверить структуру БД:**
   - Таблица `orders` должна иметь связь с `products` через `productId`
   - Таблица `products` должна иметь поле `userId` (ID продавца)

## Возможные проблемы на бэкенде

1. ❌ Заказ не связывается с продавцом при создании
2. ❌ Фильтрация заказов по роли `seller` работает неправильно
3. ❌ Токен не содержит информацию о текущем пользователе
4. ❌ Неправильная связь между таблицами `orders` и `products`

## Что сделано на фронтенде

✅ Добавлено подробное логирование для отладки
✅ Добавлено автоматическое обновление списка заказов у продавца
✅ Добавлен `sellerId` в enrichedOrder из информации о продукте
✅ Добавлены предупреждения о том, что `sellerId` не передается при создании заказа

## Следующие шаги

1. Проверить логи при создании заказа покупателем
2. Проверить логи при получении заказов продавцом
3. Убедиться, что бэкенд правильно связывает заказы с продавцом
4. При необходимости добавить передачу `sellerId` при создании заказа (если бэкенд этого требует)
