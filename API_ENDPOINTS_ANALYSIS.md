# Анализ эндпоинтов API для заказов

## 🔍 Проверка эндпоинтов на ошибки

### Базовый URL
```
http://81.3.182.146
```

### Swagger UI
```
http://81.3.182.146/swagger/index.html
```

---

## 📋 Основные эндпоинты для заказов

### 1. **GET /order/getorders?role=seller** - Получение заказов продавца

**Использование в коде:**
```296:296:lib/data/sources/api/order_api.dart
        Uri.parse('$ORDERS_URL?role=$role'),
```

**Ожидаемое поведение:**
- Бэкенд должен определить текущего пользователя из токена авторизации
- Вернуть только заказы, где товар (`productId`) принадлежит текущему продавцу
- Связь определяется через таблицу продуктов: `product.userId = seller.id`

**Возможные проблемы на сервере:**
1. ❌ **Не фильтрует заказы по продавцу** - возвращает все заказы или пустой список
2. ❌ **Неправильно определяет продавца из токена** - использует неверный userId
3. ❌ **Не связывает заказ с продавцом при создании** - заказ создается без sellerId
4. ❌ **Проблемы с авторизацией** - токен не валидируется или не содержит нужных данных

**Проверка в Swagger:**
1. Откройте Swagger UI: http://81.3.182.146/swagger/index.html
2. Найдите эндпоинт `GET /order/getorders`
3. Нажмите "Try it out"
4. Введите параметр `role=seller`
5. Добавьте токен авторизации (кнопка "Authorize")
6. Выполните запрос
7. Проверьте:
   - Код ответа (должен быть 200)
   - Формат ответа (должен быть массив заказов)
   - Фильтрацию (только заказы текущего продавца)

---

### 2. **POST /order/placeorder** - Создание заказа

**Использование в коде:**
```178:182:lib/data/sources/api/order_api.dart
      final response = await http.post(
        Uri.parse(PLACE_ORDER_URL),
        headers: headers,
        body: jsonEncode(requestBody),
      );
```

**Отправляемые данные:**
```json
{
  "userId": "ID покупателя",
  "productId": 123,
  "quantity": 1,
  "deliveryLatitude": 59.9343,
  "deliveryLongitude": 30.3351
}
```

**⚠️ ВАЖНО:** `sellerId` НЕ передается в запросе!

**Ожидаемое поведение бэкенда:**
1. ✅ Найти товар по `productId`
2. ✅ Определить `sellerId` из `product.userId`
3. ✅ Сохранить заказ с связью к продавцу в БД
4. ✅ Вернуть созданный заказ с `sellerId` в ответе

**Возможные проблемы на сервере:**
1. ❌ **Не определяет sellerId по productId** - заказ создается без связи с продавцом
2. ❌ **Не сохраняет sellerId в БД** - заказ не связан с продавцом
3. ❌ **Не возвращает sellerId в ответе** - невозможно проверить правильность создания
4. ❌ **Товар не найден** - productId не существует в БД

**Проверка в Swagger:**
1. Найдите эндпоинт `POST /order/placeorder`
2. Нажмите "Try it out"
3. Введите тестовые данные:
   ```json
   {
     "userId": "test-buyer-id",
     "productId": 1,
     "quantity": 1,
     "deliveryLatitude": 59.9343,
     "deliveryLongitude": 30.3351
   }
   ```
4. Выполните запрос
5. Проверьте ответ:
   - Код ответа (должен быть 200 или 201)
   - Наличие `sellerId` в ответе
   - Правильность `sellerId` (должен соответствовать владельцу товара)

---

### 3. **PUT/POST /order/updatestatus** - Обновление статуса заказа

**Использование в коде:**
```474:533:lib/data/sources/api/order_api.dart
      // Пробуем несколько вариантов endpoint'ов
      final urlsToTry = [
        '$ORDERS_URL/$orderId/status', // Вариант 1: стандартный REST
        '$UPDATE_ORDER_STATUS_URL', // Вариант 2: отдельный endpoint
        '$ORDERS_URL/$orderId', // Вариант 3: обновление всего заказа
      ];
```

**⚠️ ПРОБЛЕМА:** Код пробует несколько вариантов URL, что указывает на неопределенность с правильным эндпоинтом!

**Возможные проблемы на сервере:**
1. ❌ **Эндпоинт не существует** - ни один из вариантов не работает
2. ❌ **Неправильный формат запроса** - ожидает другой формат данных
3. ❌ **Нет авторизации** - требует права продавца для обновления статуса

**Проверка в Swagger:**
1. Найдите эндпоинты для обновления статуса:
   - `PUT /order/updatestatus`
   - `PUT /order/getorders/{orderId}/status`
   - `PUT /order/getorders/{orderId}`
2. Проверьте, какой из них существует
3. Проверьте формат запроса (тело запроса)
4. Проверьте авторизацию

---

### 4. **GET /order/getproducts** - Получение товаров

**Использование в коде:**
```20:23:lib/data/sources/api/order_api.dart
    final response = await http.get(
      Uri.parse(PRODUCTS_URL),
      headers: headers,
    );
```

**Возможные проблемы:**
1. ❌ **Не возвращает userId продавца** - невозможно определить владельца товара
2. ❌ **Неправильный формат ответа** - не массив объектов

---

## 🐛 Найденные проблемы в коде клиента

### 1. **КРИТИЧЕСКАЯ ОШИБКА: Токен не добавляется в order_api_new.dart**

**Файл:** `lib/data/sources/api/order_api_new.dart`

**Проблема:**
```dart
// Строка 18, 52, 103
headers['Authorization'] = 'Bearer ';  // ❌ Токен не добавлен!
```

**Должно быть:**
```dart
headers['Authorization'] = 'Bearer $token';  // ✅ Правильно
```

**Влияние:** Запросы из `order_api_new.dart` не авторизованы!

---

### 2. **Неопределенность с эндпоинтом обновления статуса**

Код пробует 3 разных варианта URL, что указывает на проблему с документацией API или реализацией на сервере.

---

## ✅ Рекомендации по проверке в Swagger

### Шаг 1: Проверка авторизации
1. Откройте Swagger UI
2. Найдите эндпоинт авторизации (обычно `/auth/login` или `/account/login`)
3. Получите токен
4. Нажмите "Authorize" и введите токен

### Шаг 2: Проверка создания заказа
1. Создайте тестовый заказ через `POST /order/placeorder`
2. Проверьте ответ - должен содержать `sellerId`
3. Запомните `orderId` и `productId`

### Шаг 3: Проверка получения заказов продавца
1. Выполните `GET /order/getorders?role=seller`
2. Проверьте, что возвращается созданный заказ
3. Проверьте, что возвращаются только заказы текущего продавца

### Шаг 4: Проверка обновления статуса
1. Найдите правильный эндпоинт для обновления статуса
2. Обновите статус созданного заказа
3. Проверьте ответ

---

## 🔧 Что проверить на сервере

### 1. Логика создания заказа (`POST /order/placeorder`)
```sql
-- Проверьте, что при создании заказа:
-- 1. Находится товар по productId
SELECT * FROM Products WHERE id = @productId;

-- 2. Определяется sellerId из товара
SELECT userId FROM Products WHERE id = @productId;

-- 3. Сохраняется заказ с sellerId
INSERT INTO Orders (userId, productId, sellerId, ...) 
VALUES (@userId, @productId, @sellerId, ...);
```

### 2. Логика получения заказов продавца (`GET /order/getorders?role=seller`)
```sql
-- Проверьте, что запрос фильтрует по продавцу:
SELECT o.* 
FROM Orders o
INNER JOIN Products p ON o.productId = p.id
WHERE p.userId = @currentSellerId  -- Текущий продавец из токена
AND o.status != 'delivered'  -- Или другая логика фильтрации
```

### 3. Валидация токена
- Проверьте, что токен правильно декодируется
- Проверьте, что из токена извлекается правильный `userId`
- Проверьте, что `role` определяется правильно

---

## 📝 Чек-лист для проверки

- [ ] Swagger UI доступен по адресу http://81.3.182.146/swagger/index.html
- [ ] Эндпоинт `GET /order/getorders?role=seller` существует
- [ ] Эндпоинт `POST /order/placeorder` существует
- [ ] Эндпоинт для обновления статуса существует и работает
- [ ] При создании заказа определяется `sellerId` по `productId`
- [ ] При запросе заказов продавца возвращаются только его заказы
- [ ] Токен авторизации правильно валидируется
- [ ] Из токена правильно извлекается `userId` и `role`

---

## 🚨 Критические проблемы, требующие исправления

1. **Исправить добавление токена в `order_api_new.dart`**
2. **Уточнить правильный эндпоинт для обновления статуса заказа**
3. **Проверить на сервере логику связывания заказа с продавцом**


