# Руководство по отладке проблемы с заказами продавца

## Проблема
Заказ, созданный покупателем, не отображается у продавца.

## Что было добавлено для отладки

### 1. Логирование при создании заказа (покупатель)

При создании заказа в консоли будут выводиться:
```
📤 СОЗДАНИЕ ЗАКАЗА ПОКУПАТЕЛЕМ
   userId: [ID покупателя]
   productId: [ID товара]
   quantity: [количество]
   Координаты: [широта], [долгота]

=== ОТПРАВКА ЗАКАЗА НА СЕРВЕР ===
URL: http://81.3.182.146/order/placeorder
Headers: {...}
Request Body: {...}
⚠️ ВНИМАНИЕ: sellerId не передается! Бэкенд должен определить продавца по productId
=== КОНЕЦ ОТПРАВКИ ЗАКАЗА ===

=== ОТВЕТ СЕРВЕРА ===
Status Code: [код ответа]
Response Body: [тело ответа]
=== КОНЕЦ ОТВЕТА СЕРВЕРА ===
```

### 2. Логирование при получении заказов (продавец)

При обновлении списка заказов у продавца:
```
🔄 Обновление списка заказов продавца...
👤 Текущий пользователь: userId=[ID], role=[роль]

=== ПОЛУЧЕНИЕ ЗАКАЗОВ ПО РОЛИ: seller ===
URL: http://81.3.182.146/order/getorders?role=seller
Headers: {...}

=== ОТВЕТ СЕРВЕРА ДЛЯ РОЛИ seller ===
Status Code: [код ответа]
Response Body: [тело ответа]
=== КОНЕЦ ОТВЕТА СЕРВЕРА ===

📦 Получено [количество] заказов для роли seller
📋 Обработка заказа ID: [ID], productId: [ID]
  ✅ Товар найден. sellerId (из продукта): [ID продавца]
✅ Загружено [количество] заказов для продавца
```

## Инструкция по проверке

### Шаг 1: Создайте заказ как покупатель

1. Войдите как покупатель
2. Выберите товар и создайте заказ
3. **Проверьте логи в консоли:**
   - Должно быть: `✅ Заказ успешно размещен!`
   - Status Code должен быть `200` или `201`
   - Если Status Code не 200/201 - проблема на бэкенде

### Шаг 2: Проверьте заказы у продавца

1. Войдите как продавец (владелец товара)
2. Перейдите на вкладку "Заказы"
3. **Проверьте логи в консоли:**
   - Должно быть: `📦 Получено X заказов для роли seller`
   - Если `X = 0`, значит бэкенд не возвращает заказы

### Шаг 3: Проверьте связь товара с продавцом

В логах должно быть:
```
📋 Обработка заказа ID: X, productId: Y
  ✅ Товар найден. sellerId (из продукта): Z
```

**Проверьте:**
- `sellerId (из продукта): Z` должен совпадать с ID текущего продавца
- Если не совпадает - товар принадлежит другому продавцу

## Возможные проблемы и решения

### Проблема 1: Status Code не 200/201 при создании заказа

**Причина:** Бэкенд не принимает заказ или возвращает ошибку

**Решение:** 
- Проверьте формат данных в Request Body
- Проверьте, что токен авторизации валиден
- Проверьте логи бэкенда

### Проблема 2: Заказов 0 у продавца

**Причина:** Бэкенд не возвращает заказы для текущего продавца

**Возможные причины:**
1. Бэкенд не связывает заказ с продавцом при создании
2. Фильтрация по роли `seller` работает неправильно
3. Токен не содержит правильную информацию о пользователе

**Решение:**
- Проверьте логи бэкенда при создании заказа
- Проверьте, что бэкенд определяет продавца по `productId`
- Проверьте SQL запрос на бэкенде: должен фильтровать по `products.userId = current_user_id`

### Проблема 3: sellerId из продукта не совпадает с текущим продавцом

**Причина:** Товар принадлежит другому продавцу

**Решение:**
- Убедитесь, что вы создаете заказ на товар, который принадлежит текущему продавцу
- Проверьте, что `product.userId` совпадает с ID продавца

## Что проверить на бэкенде

1. **Эндпоинт `/order/placeorder`:**
   - Должен автоматически определять `sellerId` по `productId`
   - Должен сохранять связь заказа с продавцом в БД
   - Должен возвращать Status Code 200 или 201

2. **Эндпоинт `/order/getorders?role=seller`:**
   - Должен фильтровать заказы по текущему пользователю (из токена)
   - Должен возвращать только заказы, где товар принадлежит текущему продавцу
   - SQL: `SELECT * FROM orders WHERE productId IN (SELECT id FROM products WHERE userId = current_user_id)`

3. **Структура БД:**
   - Таблица `orders` должна иметь связь с `products` через `productId`
   - Таблица `products` должна иметь поле `userId` (ID продавца)

## Следующие шаги

1. Запустите приложение и создайте заказ как покупатель
2. Проверьте логи в консоли
3. Войдите как продавец и проверьте логи при загрузке заказов
4. Сравните `sellerId` из продукта с ID текущего продавца
5. Если проблема сохраняется - проверьте логи бэкенда
